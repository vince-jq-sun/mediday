# Audio processing pipeline dependencies - Python 3.12 compatible versions
google-cloud-speech>=2.21.0
google-cloud-translate>=3.12.0
google-cloud-texttospeech>=2.16.3
librosa>=0.10.1
soundfile>=0.12.1
numpy>=1.24.0
pygame>=2.5.0
pydub>=0.25.1
python-dotenv>=1.0.0
tqdm>=4.66.0

# Core dependencies with specific versions for stability
setuptools>=68.0.0
wheel>=0.40.0

# Audio processing
scipy>=1.10.0
resampy>=0.4.0

# GUI dependencies (install separately if needed)
# pyaudio  # May need system-level installation
simpleaudio>=1.0.4  # For audio playback with seeking support
